---
phase: 05-configuration-versioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/reactor.ts, src/extension.ts, src/chartView.ts, src/index.ts]
autonomous: true
requirements: [CONFIG-01]

must_haves:
  truths:
    - "The 'antigravity-watcher.enableRetry' setting appears in VS Code settings"
    - "The extension respects the 'enableRetry' setting (no retries when disabled)"
    - "The extension continues to retry when the setting is enabled (default)"
  artifacts:
    - path: "package.json"
      provides: "New configuration setting"
    - path: "src/reactor.ts"
      provides: "Configurable retry logic"
  key_links:
    - from: "src/extension.ts"
      to: "src/reactor.ts"
      via: "FetchOptions"
---

<objective>
Implement the `antigravity-watcher.enableRetry` configuration toggle and update the `ReactorCore` to honor this setting across all consumers (Status Bar, Charts, and CLI).

Purpose: Give users control over the auto-retry mechanism.
Output: Configurable retry behavior and updated source code.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/01-config-retry-research.md
@package.json
@src/reactor.ts
@src/extension.ts
@src/chartView.ts
@src/index.ts

<interfaces>
From src/reactor.ts:
```typescript
export class ReactorCore {
  async fetchQuotaSnapshot(onRetry?: (attempt: number, delayMs: number) => void): Promise<QuotaSnapshot>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add enableRetry setting to package.json</name>
  <files>package.json</files>
  <action>
    Add `antigravity-watcher.enableRetry` to the `contributes.configuration.properties` section in `package.json`.
    - Type: `boolean`
    - Default: `true`
    - Description: "Enable automatic retry with exponential backoff when Antigravity servers are experiencing high traffic (429 errors)."
  </action>
  <verify>
    <automated>grep "antigravity-watcher.enableRetry" package.json</automated>
  </verify>
  <done>Configuration property added to package.json</done>
</task>

<task type="auto">
  <name>Task 2: Update ReactorCore to honor enableRetry</name>
  <files>src/reactor.ts</files>
  <action>
    Refactor `ReactorCore.fetchQuotaSnapshot` and `transmitWithRetry` to accept an optional `FetchOptions` object per research recommendations.
    
    1. Define `FetchOptions` interface:
    ```typescript
    export interface FetchOptions {
      enableRetry?: boolean;
      onRetry?: (attempt: number, delayMs: number) => void;
    }
    ```
    2. Update `fetchQuotaSnapshot(options?: FetchOptions)`
    3. Update `transmitWithRetry<T>(..., options?: FetchOptions)`
    4. In `transmitWithRetry`, set `maxRetries = options?.enableRetry !== false ? 5 : 0;`
    
    Maintain backward compatibility or update all call sites if necessary.
  </action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <done>ReactorCore updated to support configurable retries</done>
</task>

<task type="auto">
  <name>Task 3: Update consumers to pass the configuration</name>
  <files>src/extension.ts, src/chartView.ts, src/index.ts</files>
  <action>
    Update all call sites of `fetchQuotaSnapshot` to read the setting from VS Code configuration and pass it to the reactor.
    
    - In `src/extension.ts`: Read `enableRetry` using `vscode.workspace.getConfiguration('antigravity-watcher').get('enableRetry', true)`.
    - In `src/chartView.ts`: Same as extension.ts.
    - In `src/index.ts`: Pass `{ enableRetry: true }` (or false if preferred for CLI) as a default.
    
    Ensure `onRetry` callbacks are correctly wrapped into the `options` object.
  </action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <done>All consumers updated to honor the new setting</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User Settings → API Client | Untrusted user configuration values are passed to the retry logic |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | package.json | mitigate | Use type-checked configuration access via VS Code API |
</threat_model>

<verification>
1. Run `npm run compile` to ensure no type errors.
2. Manually verify in Extension Development Host that the setting exists and can be toggled.
</verification>

<success_criteria>
- Setting `antigravity-watcher.enableRetry` exists in package.json.
- `ReactorCore` uses the setting to decide whether to retry.
- Extension compiles and runs without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/05-configuration-versioning/05-01-SUMMARY.md`
</output>
