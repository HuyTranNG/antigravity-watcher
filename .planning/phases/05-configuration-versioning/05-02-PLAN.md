---
phase: 05-configuration-versioning
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [package.json, CHANGELOG.md]
autonomous: true
requirements: [VERSION-01]

must_haves:
  truths:
    - "Extension version is 1.0.6"
    - "CHANGELOG.md contains entry for 1.0.6"
  artifacts:
    - path: "package.json"
      provides: "Updated version string"
    - path: "CHANGELOG.md"
      provides: "Release notes for 1.0.6"
---

<objective>
Upgrade the extension version to 1.0.6 and document the new configuration toggle in the changelog.

Purpose: Maintain release history and signal new capabilities.
Output: Updated package metadata and changelog.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade version to 1.0.6</name>
  <files>package.json</files>
  <action>
    Update the `version` field in `package.json` from `1.0.5` to `1.0.6`.
  </action>
  <verify>
    <automated>grep '"version": "1.0.6"' package.json</automated>
  </verify>
  <done>Version upgraded in package.json</done>
</task>

<task type="auto">
  <name>Task 2: Update CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <action>
    Add a new entry for version `1.0.6` at the top of the changelog.
    Include the following details:
    - **Added**: `antigravity-watcher.enableRetry` setting to toggle automatic retries for high-traffic errors.
    - **Changed**: `ReactorCore` refactored to support configurable fetch options.
  </action>
  <verify>
    <automated>grep "## \[1.0.6\]" CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG.md updated with release notes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
None (metadata update only).

## STRIDE Threat Register
None applicable.
</threat_model>

<verification>
1. Verify `package.json` has version `1.0.6`.
2. Verify `CHANGELOG.md` has the new entry.
</verification>

<success_criteria>
- package.json version is 1.0.6.
- CHANGELOG.md contains 1.0.6 entry with details of the new setting.
</success_criteria>

<output>
After completion, create `.planning/phases/05-configuration-versioning/05-02-SUMMARY.md`
</output>
