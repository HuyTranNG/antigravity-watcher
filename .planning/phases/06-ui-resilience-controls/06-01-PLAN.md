---
phase: "06"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["src/extension.ts", "package.json"]
autonomous: true
requirements: ["UI-06-01"]
must_haves:
  truths:
    - "Status Bar shows a retry toggle icon ($(sync) for enabled, $(sync-ignored) for disabled)"
    - "Clicking the retry icon toggles the 'antigravity-watcher.enableRetry' setting"
    - "Icon updates reactively when the setting is changed in VS Code Settings"
  artifacts:
    - path: "src/extension.ts"
      provides: "New retry status bar item and toggle command"
    - path: "package.json"
      provides: "New toggle retry command definition"
---

<objective>
Implement a Status Bar toggle for the retry feature.

Purpose: Provide a quick way to toggle retry without opening settings.
Output: A new status bar item that shows status and allows toggling.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-resilience-controls/06-CONTEXT.md
@src/extension.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define toggleRetry command in package.json</name>
  <files>package.json</files>
  <action>
    Add "antigravity-watcher.toggleRetry" to the "commands" section of package.json.
    Label: "Antigravity Watcher: Toggle Retry"
  </action>
  <verify>
    <automated>grep "antigravity-watcher.toggleRetry" package.json</automated>
  </verify>
  <done>Command is registered in package.json</done>
</task>

<task type="auto">
  <name>Task 2: Implement retry toggle in src/extension.ts</name>
  <files>src/extension.ts</files>
  <action>
    1. Register the `antigravity-watcher.toggleRetry` command:
       - Read current `antigravity-watcher.enableRetry` setting.
       - Toggle the value (true -> false, false -> true).
       - Update the configuration globally using `config.update('enableRetry', newValue, vscode.ConfigurationTarget.Global)`.
    2. Create a second status bar item `retryStatusBarItem`:
       - Alignment: Right, priority: 101 (to be to the left of the existing quota item which has priority 100).
       - Command: `antigravity-watcher.toggleRetry`.
       - Initial text based on current `enableRetry` config: `$(sync)` for enabled, `$(sync-ignored)` for disabled.
       - Tooltip: "Toggle Auto-Retry (Current: [Enabled/Disabled])".
    3. Update `retryStatusBarItem` in `onDidChangeConfiguration` when `enableRetry` changes.
    4. Call update logic initially.
  </action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <done>Status bar item is created, command is implemented, and reactive updates are wired up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings UI → Extension | Configuration values updated by user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Configuration | mitigate | Use VS Code's configuration API which handles permissions and sanitization |
</threat_model>

<success_criteria>
- A new status bar item appears next to the quota info.
- It shows $(sync) when retry is enabled.
- It shows $(sync-ignored) when retry is disabled.
- Clicking it toggles the setting and the icon updates.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-resilience-controls/06-01-SUMMARY.md`
</output>
