---
phase: "06"
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: ["src/chartView.ts"]
autonomous: true
requirements: ["UI-06-02"]
must_haves:
  truths:
    - "Webview header contains a retry toggle (switch or button)"
    - "Toggling the webview retry control updates the global VS Code setting"
    - "Webview toggle state reflects the global VS Code setting reactively"
  artifacts:
    - path: "src/chartView.ts"
      provides: "New retry toggle in the webview header and message handling"
---

<objective>
Implement a Sidebar (Webview) toggle and status indicator for the retry feature.

Purpose: Allow the user to toggle retry directly from the Sidebar.
Output: A toggle control in the webview header that stays in sync with the extension's configuration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-resilience-controls/06-CONTEXT.md
@src/chartView.ts
@src/extension.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add retry toggle HTML/CSS to webview</name>
  <files>src/chartView.ts</files>
  <action>
    1. Modify `_getHtmlForWebview` in `src/chartView.ts`:
       - Add a container in the header for the retry toggle.
       - Use a checkbox with a switch-like CSS or a simple button.
       - Add a label "Retry".
    2. Add CSS for the toggle (switch style is preferred for a better UI).
    3. Add JavaScript to the webview to post a message `type: 'toggleRetry'` when clicked.
    4. The toggle state (checked/unchecked) should be derived from the passed `enableRetry` configuration.
  </action>
  <verify>
    <automated>grep "toggleRetry" src/chartView.ts</automated>
  </verify>
  <done>Webview HTML and CSS include the retry toggle.</done>
</task>

<task type="auto">
  <name>Task 2: Handle toggleRetry message and reactive updates</name>
  <files>src/chartView.ts</files>
  <action>
    1. In `resolveWebviewView`'s `onDidReceiveMessage`:
       - Handle `toggleRetry`.
       - Execute `vscode.commands.executeCommand('antigravity-watcher.toggleRetry')`.
    2. Add a listener for configuration changes in `resolveWebviewView`:
       - `vscode.workspace.onDidChangeConfiguration(e => { if (e.affectsConfiguration('antigravity-watcher.enableRetry')) { this.updateView(); } })`.
       - Ensure it's pushed to `webviewView.onDidDispose`.
    3. Ensure `updateView` passes the current `enableRetry` config value to `_getHtmlForWebview`.
  </action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <done>Webview handles the toggle message and updates reactively when the setting changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Webview → Extension | Messages sent from webview to extension |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Elevation of Privilege | onDidReceiveMessage | mitigate | Only handle specific types of messages ('refresh', 'toggleRetry') and use them to trigger existing, safe commands |
</threat_model>

<success_criteria>
- A toggle switch or button labeled "Retry" appears in the sidebar header next to "Refresh".
- Clicking it toggles the retry feature (same effect as the status bar item).
- The toggle state updates when the setting is changed elsewhere.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-resilience-controls/06-02-SUMMARY.md`
</output>
